---
/**
 * 轻量「如今稀缺的」状态面板 —— 一眼可读，无需解释：
 * 知 / 品 / 行 三行（单词 + 一句话注解，无状态徽章，无产品名）。
 * 底部一条杠杆线：you ──► company-scale。
 *
 * 全浅色：白卡 + mint 细线，无深色块。装饰性，aria-hidden。
 * 动效由 motion.ts 的 heroStack() 编排：行交错入场、杠杆线绘制 + mint 脉冲、指针物理按压。
 */
import { getDict } from '../i18n';
const s = getDict(Astro.currentLocale).stack;
---

<div class="stack-stage" data-hero-stack aria-hidden="true">
  <div class="stack" data-hs-tilt>
    <div class="stack-head">
      <span class="stack-head-label">{s.headLabel}</span>
    </div>

    {s.rows.map((l) => (
      <div class="stack-row" data-hs-row>
        <span class="stack-idx">{l.idx}</span>
        <span class="stack-main">
          <span class="stack-name">{l.name}</span>
          <span class="stack-gloss">{l.gloss}</span>
        </span>
      </div>
    ))}

    <div class="stack-lever">
      <span class="lever-label">{s.lever.you}</span>
      <span class="lever-line" data-hs-line>
        <span class="lever-pulse" data-hs-pulse></span>
      </span>
      <span class="lever-label is-accent">{s.lever.companyScale}</span>
    </div>
  </div>
</div>

<style>
  /* 透视舞台：仅作结构容器。透视由 GSAP transformPerspective 内联提供，
     避免与父级 perspective 叠加造成双重畸变/抖动 */
  .stack-stage {
    width: 100%;
    max-width: 440px;
    margin-inline: auto;
  }
  .stack {
    transform-style: preserve-3d;
    --card-lift: 12px; /* 阴影抬升量（motion.ts 动态驱动） */
    border: 1px solid var(--color-mint-soft);
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.85);
    box-shadow:
      0 1px 0 rgba(10, 108, 75, 0.04),
      0 var(--card-lift, 12px) 24px -18px rgba(10, 108, 75, 0.16);
  }

  .stack-head {
    padding: 13px 18px;
    border-bottom: 1px solid var(--color-hairline-soft);
  }
  .stack-head-label {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-steel);
  }

  .stack-row {
    display: grid;
    grid-template-columns: 26px 1fr;
    align-items: center;
    gap: 10px;
    padding: 14px 18px;
  }
  .stack-row + .stack-row {
    border-top: 1px solid var(--color-hairline-soft);
  }
  .stack-idx {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--color-steel);
  }
  .stack-main {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 12px;
    min-width: 0;
  }
  .stack-name {
    font-family: var(--font-display);
    font-size: 22px;
    line-height: 1.2;
    color: var(--color-ink);
  }
  .stack-gloss {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.02em;
    color: var(--color-steel);
    text-align: right;
  }

  /* 底部杠杆线 */
  .stack-lever {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 18px 16px;
    border-top: 1px dashed var(--color-mint-soft);
  }
  .lever-label {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-primary);
  }
  .lever-label.is-accent {
    color: var(--color-primary-deep);
    font-weight: 600;
  }
  .lever-line {
    flex: 1;
    height: 1.5px;
    border-radius: 1px;
    background: var(--color-hairline-soft);
    position: relative;
    overflow: hidden;
    transform-origin: left center;
  }
  .lever-pulse {
    position: absolute;
    left: 0;
    top: 0;
    height: 100%;
    width: 42%;
    border-radius: 1px;
    background: linear-gradient(90deg, var(--color-mint), var(--color-primary));
    transform-origin: left center;
  }
</style>
